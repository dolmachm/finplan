export { prisma, type InputJsonValue, type Db } from "@/shared/db/index";
export type {
  AccountStatus, UserRole, AssetType, LiabilityType, IncomeSource,
  Frequency, ScenarioKind, JobStatus,
} from "@/shared/types";
